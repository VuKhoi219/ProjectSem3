@model Project_Sem3.Models.Payment

@{
    ViewData["Title"] = "Delete";
}

@section VendorStyles {
<!-- Add any vendor stylesheets you might still need -->
<link rel="stylesheet" href="~/vendor/libs/datatables-bs5/datatables.bootstrap5.css">
<link rel="stylesheet" href="~/vendor/libs/datatables-responsive-bs5/responsive.bootstrap5.css">
}

@section VendorScripts {
<!-- Add any scripts you need for this page -->
<script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script>
}

<h1>Delete</h1>

<h3>Are you sure you want to delete this?</h3>
<div>
  <h4>Payment</h4>
  <hr />
  <dl class="row">
    <dt class = "col-sm-2">
      @Html.DisplayNameFor(model => model.Amount)
    </dt>
    <dd class = "col-sm-10">
      @Html.DisplayFor(model => model.Amount)
    </dd>
    <dt class = "col-sm-2">
      @Html.DisplayNameFor(model => model.PaymentDate)
    </dt>
    <dd class = "col-sm-10">
      @Html.DisplayFor(model => model.PaymentDate)
    </dd>
    <dt class = "col-sm-2">
      @Html.DisplayNameFor(model => model.Status)
    </dt>
    <dd class = "col-sm-10">
      @Html.DisplayFor(model => model.Status)
    </dd>
    <dt class = "col-sm-2">
      @Html.DisplayNameFor(model => model.ImageUrl)
    </dt>
    <dd class = "col-sm-10">
      @Html.DisplayFor(model => model.ImageUrl)
    </dd>
    <dt class = "col-sm-2">
      @Html.DisplayNameFor(model => model.Contract)
    </dt>
    <dd class = "col-sm-10">
      @Html.DisplayFor(model => model.Contract.Id)
    </dd>
  </dl>

  <form asp-action="Delete">
    <input type="hidden" asp-for="Id" />
    <input type="submit" value="Delete" class="btn btn-danger" /> |
    <a asp-action="Index">Back to List</a>
  </form>
</div>

@section Scripts {
<script src="https://ajax.googleapis.com/ajax/libs/jquery/3.6.0/jquery.min.js"></script>
<script src="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js"></script>
<script>
  $(document).ready(function() {
    // Xử lý hành động xóa khi nút xóa được nhấn
    $('#deleteButton').on('click', function(e) {
      e.preventDefault(); // Ngừng hành động mặc định

      // Lấy ID của payment (giả sử nó được lưu trong thuộc tính data của nút)
      const paymentId = $(this).data('payment-id');

      // Gửi yêu cầu AJAX để xóa payment
      $.ajax({
        url: '/api/payments/' + paymentId,  // Giả sử bạn sử dụng paymentId trong URL
        method: 'DELETE',
        success: function(response) {
          $('#successAlert').show();
          $('#errorAlert').hide();
          // Tùy chọn: loại bỏ payment đã xóa khỏi DOM
          $('#paymentRow_' + paymentId).remove();
        },
        error: function(xhr, status, error) {
          $('#errorAlert').show();
          $('#successAlert').hide();
        }
      });
    });
  });
</script>
}


